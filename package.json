{
  "name": "ozzy",
  "version": "0.2.0",
  "description": "A lightweight API interface",
  "main": "dist/index.js",
  "files": [
    "dist",
    "LICENSE",
    "README.md",
    "package.json"
  ],
  "repository": {
    "url": "git+https://github.com/duncangrubbs/ozzy.git"
  },
  "author": "Duncan Grubbs <duncan.grubbs@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc --p tsconfig.production.json",
    "test": "echo \"ok\"",
    "example": "rm -rf dist.examples && tsc --p tsconfig.examples.json --outDir dist.examples && node dist.examples/examples/basic.js",
    "prettier": "prettier --write --single-quote src/"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "typescript": "^5.4.5"
  }
}
